collection_name: oge
# 核心修正：移除 default: default，仅保留连接参数
connection_args:
  uri: http://192.168.181.134:19530
  db_name: test
  user: root
  password: root
auto_id: True
# 新增必传参数：向量维度（与你的 embed_model 输出一致，如 text-embedding-v4 是 1024）
vector_dim: 1024
# 新增必传参数：相似度算法（L2/IP，二选一）
metric_type: L2



data_path: data/basic_info
allow_knowledge_file_type: ["txt", "pdf", "html"]

chunk_size: 500
chunk_overlap: 50
separators: ["\n\n", "\n", ".", "!", "?", "。", "！", "？", " ", ""]
k: 3